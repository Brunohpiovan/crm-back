package com.juridiqsystem.crm.model.enums;

/** Situação processual local — derivada do campo "situacao" da capa (ProcessoFonteCapa) retornada pela Escavador. */
public enum ProcessoSituacao {
    ATIVO,
    INATIVO,
    ARQUIVADO,
    DESCONHECIDO
}
