package com.juridiqsystem.crm.model.enums;

/** Papel de um envolvido num Processo — derivado do campo "polo"/"tipo_normalizado" da Escavador (Envolvido). */
public enum TipoEnvolvido {
    AUTOR,
    REU,
    ADVOGADO,
    TERCEIRO,
    DESCONHECIDO
}
